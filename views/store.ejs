<!DOCTYPE html>
<html>
    <head>
        <title>Campus Bites - Food Basket/Payment</title>
        <meta name="description" content="This is the description">
        <link rel="stylesheet" href="styles.css" />
        <script src="https://checkout.stripe.com/checkout.js" defer></script>
        <script>
            var stripePublicKey = '<%= stripePublicKey %>'
        </script>
    </head>
    <body>
        <header class="main-header">
            <h1>Campus Bites</h1>
        </header>
        <section class="container content-section">
            <h2 class="section-header">All Food</h2>
            <div class="shop-items">
                <% items.food.forEach(function(item){ %>
                    <div class="shop-item" data-item-id="<%= item.id %>">
                        <span class="shop-item-fooditem"><%= item.fooditem %></span>
                        <div class="shop-item-details">
                            <span class="shop-item-foodtype"><%= item.foodtype %></span>
                            <span class="shop-item-price">£<%= item.price / 100 %></span>
                            <button class="btn btn-primary shop-item-button" type="button">ADD TO CART</button>
                        </div>
                    </div>
                <% }) %>
            </div>
        </section>
        <section class="container content-section">
            <h2 class="section-header">CART</h2>
            <div class="cart-row">
                <span class="cart-item cart-header cart-column">ITEM</span>
                <span class="cart-price cart-header cart-column">PRICE</span>
                <span class="cart-quantity cart-header cart-column">QUANTITY</span>
            </div>
            <div class="cart-items">
            </div>
            <div class="cart-total">
                <strong class="cart-total-title">Total</strong>
                <span class="cart-total-price">£0</span>
            </div>
            <button class="btn btn-primary btn-purchase" type="button">PURCHASE</button>
        </section>
        <script src="store.js" defer></script>
    </body>
</html>